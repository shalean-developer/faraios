export const PLATFORM_WORKSPACE_COOKIE = "faraios_platform_ws";

export const PLATFORM_WORKSPACE_SESSION_ACTIONS = {
  enter: "workspace.enter",
  exit: "workspace.exit",
  activity: "workspace.activity",
} as const;
