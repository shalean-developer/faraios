/**
 * @deprecated Import from `@/lib/industry-modules/loader` instead.
 * Kept for backward compatibility with existing imports.
 */
export {
  getBookingFormPreset as getIndustryBookingPreset,
  INDUSTRY_PRESET_SLUGS,
  listIndustryModuleSlugs,
  mergeBookingFormFields,
} from "@/lib/industry-modules/loader";
